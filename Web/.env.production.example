# Production build (served behind nginx on the same domain as /api).
# Copy to .env.production on the VPS before running `npm run build`.

# Empty/relative — nginx proxies /api/* to the Server on the same origin.
VITE_API_URL=
VITE_TENANT_ID=patan
